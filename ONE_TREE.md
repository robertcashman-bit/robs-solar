# One tree

This repository is the **only** Rob's Finance source of truth.

| | |
|---|---|
| **Git** | `robertcashman-bit/robs-solar` on `main` |
| **Cloud** | https://robs-solar.vercel.app — same commit, same tree |
| **Imported from** | Overnight All monorepo `robs-solar/` (`ab99cc745c2abb5ae31ea1c045aa91661c5123ee`) |

Do not develop or deploy a second copy from `All/robs-solar` or a stale local snapshot. Local and hosted are this folder.

The All GitHub repo (`robertdavidcashman-droid/All`) is not reachable from this machine's GitHub login. The overnight finance tree was imported from the last All production deploy, then kept deployable on Vercel with `backend/main.py` (`main:app`) and Neon.
